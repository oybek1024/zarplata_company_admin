apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: muno_admin
  name: muno_admin
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: muno_admin
  template:
    metadata:
      labels:
        app: muno_admin
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: apps
                    operator: In
                    values:
                      - "true"
      imagePullSecrets:
        - name: gitlab-registry
      containers:
        - env:
            - name: PORT
              value: ":7077"
            - name: BASE_URL
              valueFrom:
                secretKeyRef:
                  name: muno_admin-secrets
                  key: base_url
          image: gitlab.udevs.io:5050/muno/muno_admin:latest
          name: muno_admin
          imagePullPolicy: Always
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: muno_admin
  namespace: microservices
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: muno_admin
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: muno_admin_ingress
  namespace: microservices
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: admin.muno.uz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: muno_admin
                port:
                  number: 80
